<%# ICODOS branding: the logo already contains the "ICODOS" wordmark, so we %>
<%# render the logo only — the stock partial's separate <span>DocuSeal</span> is dropped. %>
<%= render 'shared/logo' %>
